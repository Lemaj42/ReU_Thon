{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('styles/Partials/footer.css') }}">
{% endblock %}

<footer class="site-footer">
	<div class="container">
		<div class="row align-items-center">
			<div class="col-md-6 text-center text-md-start mb-3 mb-md-0">
				<a href="{{ path('app_meeting_index') }}" class="footer-brand d-inline-flex align-items-center">
					<img src="{{ asset('images/logo.svg') }}" alt="Logo" class="footer-logo">
					<span>ReU-Thon</span>

				</a>
			</div>
			<div class="col-md-6 text-center text-md-end">
				<nav>
					<ul class="footer-nav list-unstyled d-flex flex-wrap justify-content-center justify-content-md-end">
						<li class="nav-item">
							<a href="{{ path('app_user_contact') }}" class="footer-link">CONTACT</a>
						</li>
						<li class="nav-item">
							<a href="{{ path('app_user_cookies') }}" class="footer-link">POLITIQUE DES COOKIES</a>
						</li>
						<li class="nav-item">
							<a href="{{ path('app_meeting_index') }}" class="footer-link">BLABLA</a>
						</li>
					</ul>
				</nav>
			</div>
		</div>
	</div>
</footer>
